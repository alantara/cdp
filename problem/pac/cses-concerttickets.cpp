#include <bits/stdc++.h>

#define MAX 212345

int f[MAX], t[MAX], c[MAX];
int i, n, m;

int main(){
  scanf("%d %d", &n, &m);

  for(i = 0; i < n; i++){
    scanf("%d", &t[i]);
    f[i]=1;
  }
  sort(t, t+n);

  for(i = 0; i < m; i++){
    
  }
}